


// Simple Function Declartion
function addition(){
    var a = Number(prompt('Enter the value of a'));  
    var b = Number(prompt('Enter the value of b'));

    var result = a+b;
    console.log(result);
}

// addition();  // to call function

function multiple(a, b){

    var result = a*b;
    console.log(result);
}